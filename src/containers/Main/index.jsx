import React, { Component } from 'react'
import './index.scss'

export default class Main extends Component {
    render () {
        return <div>
            1111233
            {this.props.children}
        </div>
    }
}
